BENCH_NAMESPACE=slurm-bench          # k8s namespace for benchmark jobs
BENCH_RESULTS_DIR=results            # output directory for run artifacts
BENCH_REPETITIONS=3                  # runs per scenario
BENCH_POLL_INTERVAL_S=5              # seconds between job status polls